
// Area of triangle when base and heigth is known

const height = +prompt("Enter the height");
const base = +prompt("Enter the base")

const Area = (base * height)/2;


console.log(`Area of given triangle properties ${height} and ${base} is ${Area}`);

